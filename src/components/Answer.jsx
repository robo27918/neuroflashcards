import React from "react";
/**
 * 
 * @param: answer to the front side of the question 
 * @returns 
 */
const Answer = (props)=>{
    return (
        <div>
            <h3>{props.answer}</h3>
        </div>
    );
}
export default Answer;